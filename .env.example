# 1. OPTION A: SMTP Configuration (Recommended for Vercel/Netlify)
# Use this if you have an external SMTP service.
# For GMAIL, see 'docs/gmail_setup_guide.md' for password instructions.
SMTP_HOST=smtp.gmail.com   # e.g., smtp.gmail.com
SMTP_PORT=587              # 465 (SSL) or 587 (TLS)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password  # NOTE: Use 'App Password', NOT your login password
SMTP_FROM_EMAIL=your-email@gmail.com # The address sending the email
SMTP_FROM_NAME="Suksan Massage" # The display name

# 3. SMS Gateway Configuration
# The email address provided by your SMS provider
# Use <number> as a placeholder if you want to swap it dynamically (not typical, usually fixed)
# But for typical gateways like mailsms.ch, it is often: number@provider.ch
MAIL2SMS_ADDRESS=079xxxxxxx@mailsms.chADMIN_PASSWORD=change_me_in_production
